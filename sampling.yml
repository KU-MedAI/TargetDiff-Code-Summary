model:
  checkpoint: ./logs_diffusion_full/targetdiff_cjkim_full_gpu/checkpoints/62000.pt

data:
  split: ./data/crossdocked_pocket10_pose_split.pt
  lmdb: ./data/crossdocked_v1.1_rmsd1.0_pocket10_processed_final.lmdb

sample:
  seed: 2021
  num_samples: 100
  num_steps: 1000
  pos_only: False
  center_pos_mode: protein
  sample_num_atoms: prior
